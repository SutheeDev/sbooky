import React from 'react'
import BookList from './Book'

const Products = () => {
    return (
        <section>
            <BookList/>
            <BookList/>
            <BookList/>
        </section>
    )
};

export default Products